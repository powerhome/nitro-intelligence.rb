name: nitro_intelligence

on:
  push:

jobs:
  ruby:
    uses: powerhome/github-actions-workflows/.github/workflows/ruby-gem.yml@d32c4e462fff67c35724ea761af72e74f5e1d32f # main
    with:
      package: ${{ github.workflow }}
      workdir: .
      ruby: '["3.3"]'
    secrets: inherit
