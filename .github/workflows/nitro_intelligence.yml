name: nitro_intelligence

on:
  push:

jobs:
  ruby:
    uses: powerhome/github-actions-workflows/.github/workflows/ruby-gem.yml@4143b5fd2df506221b14b7bb334f680af37dbd64 # main
    with:
      package: ${{ github.workflow }}
      workdir: .
      ruby: '["3.3"]'
    secrets: inherit
